apiVersion: v1
kind: Service
metadata:
  name: item-service
  namespace: apps
  labels:
    app.kubernetes.io/name: item-service
    app.kubernetes.io/part-of: kind-study
spec:
  selector:
    app.kubernetes.io/name: item-service
  ports:
    - name: http
      port: 9898
      targetPort: http
