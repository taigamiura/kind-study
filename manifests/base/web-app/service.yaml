apiVersion: v1
kind: Service
metadata:
  name: web-app
  namespace: apps
  labels:
    app.kubernetes.io/name: web-app
    app.kubernetes.io/part-of: kind-study
spec:
  selector:
    app.kubernetes.io/name: web-app
  ports:
    - name: http
      port: 80
      targetPort: http
