apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: apps
  labels:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/part-of: kind-study
spec:
  selector:
    app.kubernetes.io/name: user-service
  ports:
    - name: http
      port: 9898
      targetPort: http
