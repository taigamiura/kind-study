global:
  proxy:
    autoInject: enabled

pilot:
  autoscaleEnabled: false
  replicaCount: 1
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

meshConfig:
  accessLogFile: /dev/stdout
