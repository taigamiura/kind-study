apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: apps-local-tls
  namespace: apps
spec:
  secretName: apps-local-tls
  issuerRef:
    name: apps-selfsigned
    kind: Issuer
  commonName: app.localtest.me
  dnsNames:
    - app.localtest.me
